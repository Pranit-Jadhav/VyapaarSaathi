# Don't set VITE_API_URL in dev — the Vite proxy in vite.config.js
# handles forwarding /health, /record, /entries, etc. to port 8000.
# This avoids CORS issues (405 Method Not Allowed on OPTIONS preflight).
